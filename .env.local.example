# 作品集 AI 分身聊聊功能用的環境變數。
# 本機開發：複製成 .env.local 並填值（.env.local 已被 gitignore）。
# 線上：在 Vercel → Project → Settings → Environment Variables 設定。

# --- LLM（預設 Gemini 免費；訪客可自由切 Claude，但有花費上限）---
# Gemini 金鑰（免費、免綁卡）：https://aistudio.google.com/apikey
GEMINI_API_KEY=
# 可選，預設 gemini-2.5-flash-lite（每日免費額度較高）
GEMINI_MODEL=gemini-2.5-flash-lite

# --- Claude（選用，訪客可自由切換）---
# 申請（需綁卡儲值）：https://console.anthropic.com/
# ⚠ 強烈建議在 Console 另開一個 Workspace 並設「每月支出上限 $1」，用那個 workspace 的 key。
#    那才是唯一的硬保證，下面的預算控管只是讓你根本碰不到硬上限。
ANTHROPIC_API_KEY=
# 可選，預設 claude-haiku-4-5（最便宜，$1/$5 每百萬 token）；要更高品質可改 claude-opus-4-8（$5/$25）
CLAUDE_MODEL=claude-haiku-4-5
# 可選，Claude 單則回答的輸出上限（預設 800 token）
CLAUDE_MAX_TOKENS=800

# --- Claude 花費上限（需要 Supabase 才能跨實例準確累計，見下方）---
# 本期預算（美金），預設 1。累計花費達標後自動改用 Gemini 回答。
CLAUDE_BUDGET_USD=1
# 預算期間：month（預設，每月 1 號重置）或 total（用完就不再重置）
CLAUDE_BUDGET_WINDOW=month
# 同一個 IP 24 小時內最多幾則 Claude，預設 10；設 0 表示不限
CLAUDE_DAILY_LIMIT_PER_IP=10
# 可選，IP 雜湊用的鹽（沒設會沿用 service_role key）
CHAT_IP_SALT=

# 管理密鑰（自己設一組）。網頁網址帶 ?chatKey=<這組密鑰> 開一次即可，
# 之後這台瀏覽器用 Claude 不受預算 / 速率限制。訪客不需要也不會看到它。
CHAT_ADMIN_TOKEN=

# --- 對話紀錄 + 花費帳本（寫進 Supabase）---
# 沒填這兩個就不記錄，Claude 的花費也只能靠單一實例的記憶體粗估（此時務必依賴 Console 上限）。
# 需先建表：跑 supabase/migrations/（見 supabase/README.md）。
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
